{
  "name": "git_api_energyner",
  "version": "1.0.0",
  "description": "API modular desplegable en Render",
  "main": "src/server/index.mjs",
  "type": "module",

  "scripts": {
    "start": "node src/server/index.mjs",
    "dev": "nodemon src/server/index.mjs",
    "build": "echo \"No build step required\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "keywords": ["api", "node", "express", "render"],
  "author": "energyner",
  "license": "ISC",

  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },

  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
