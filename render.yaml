services:
  - type: web
    name: api-modular-render  
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
